# 面试案例：服务器重启后无人机无法自动恢复在线

## 问题严重性

一次户外联调测试，我们后端、嵌入式、硬件、测试四个团队十几号人，带着设备开车到郊区空旷场地。服务器部署在便携工作站上，户外电源不稳定导致服务器意外重启。重启后**所有无人机全部离线**，而且**没有自动恢复机制**——必须有人拿着电脑跑到无人机旁边，手动触发配对流程。

现场四个团队全在等我们后端排查。飞行控制、视频回传、OSD 遥测、航线规划——**没有一个功能能跑通**，因为一切依赖无人机在线。当时压力非常大，所有人的测试进度卡在我们这里。

## 定位过程

1. 查服务器日志，确认重启时间点和 MQTT broker 的连接中断时间完全吻合
2. 检查硬件端日志，发现 broker 断连后硬件处于"等待连接"的悬空状态，没有任何重试逻辑
3. 确认根因：**服务器重启后，没有机制通知硬件重新上线**。硬件团队没有做自动重连，服务器端也没有主动下发重连指令

## 解决方案

利用 Spring Event 监听 `ApplicationReadyEvent`（服务启动就绪事件），服务启动完成后通过 MQTT 向所有已注册硬件广播一条自定义重连指令。硬件收到后触发上线流程，等同于一次远程"重启上线"。

同时用 Redis 标记设备状态，区分两种下线场景：
- 主动关机（模块正常关闭）：标记为主动下线，不触发重连
- 异常断连（服务器重启、网络中断）：触发自动重连流程

## 效果

方案上线后，服务器重启导致的无人机离线问题彻底解决。再次户外测试时，服务重启后无人机在 30 秒内自动恢复在线，不需要人工介入。

## 反思

回头看这个问题的根因是**硬件端没有内置自动重连机制**，软件侧本不应该兜这个底。但在小团队做 IoT 产品，软硬件边界模糊是常态。这次经历让我学会了一件事：不要把稳定性依赖于硬件的行为，要在服务端做防御性的容错设计。同时积累了与嵌入式、硬件团队联合排障的跨团队协作经验。

---

## 纯文本版（可直接复制粘贴）

```
一次户外联调测试，我们后端、嵌入式、测试三个团队5-6号人，带着设备开车到郊区空旷场地。服务器部署在便携工作站上，笔记本电源不稳定导致服务器意外重启。重启后所有无人机全部离线，而且没有自动恢复机制——必须有人拿着电脑跑到无人机旁边，手动触发配对流程。

现场三个团队全在等我们后端排查。飞行控制、视频回传、OSD遥测、航线规划——没有一个功能能跑通，因为一切依赖无人机在线。压力非常大。

定位过程：查服务器日志确认重启时间点和MQTT broker断连时间完全吻合；检查硬件端日志，发现broker断连后硬件处于"等待连接"悬空状态，没有重试逻辑。根因是服务器重启后没有机制通知硬件重新上线。

解决方案：利用Spring Event监听ApplicationReadyEvent，服务启动完成后通过MQTT向所有已注册硬件广播一条自定义重连指令，触发上线流程。同时用Redis标记设备状态，区分主动关机（正常操作）和异常断连（触发重连）。

效果：服务重启后无人机在30秒内自动恢复在线，不再需要人工介入。

反思：根因是硬件端没有内置自动重连机制，软件侧本不应该兜这个底。但在小团队做IoT产品软硬件边界模糊是常态。这次经历让我学会了不要把稳定性依赖于硬件的行为，要在服务端做防御性容错设计。
```
